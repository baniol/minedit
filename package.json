{
  "name": "minedit",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "node index.js compile",
    "monit": "nohup node index.js > /dev/null 2>&1 & echo $! > run.pid",
    "start": "node index.js",
    "stop": "kill -9 \"$(< run.pid)\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "marked": "^0.3.5",
    "mkdirp": "^0.5.1",
    "recursive-readdir": "^1.3.0",
    "watch": "^0.16.0"
  }
}
